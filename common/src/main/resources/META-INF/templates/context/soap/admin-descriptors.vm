<${contextNamespacePrefix}:admin-descriptors #soapInsertConditionalRoot(${isRootResources} "false" "false" ${contextNamespacePrefix} ${contextNamespace} false ${LAST_MODIFICATION_DATE})>
#foreach( ${admDesc} in ${admsContent} )
${admDesc}
#end
</${contextNamespacePrefix}:admin-descriptors>