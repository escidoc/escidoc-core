#foreach ( $aggregationTableIndex in $aggregationTableIndexes )
#set( $indexName = ${aggregationTableIndex.name} )
#set( $indexFields = ${aggregationTableIndex.aggregationTableIndexFields} )
#parse("aggregation-definition-new/soap/aggregation-table-index.vm")
    
#end
