#parse("common/macros.vm")<${aggregationDefinitionNamespacePrefix}:aggregation-definition #restInsertConditionalRoot(${isRootAggregationDefinition} "true" "true" ${aggregationDefinitionNamespacePrefix} ${aggregationDefinitionNamespace} "#escA(${aggregationDefinitionName})" ${aggregationDefinitionHref})>
    <${aggregationDefinitionNamespacePrefix}:properties>
        #creationDate(${aggregationDefinitionCreationDate})
        #restCreatedBy("#escA(${aggregationDefinitionCreatedByTitle})" ${aggregationDefinitionCreatedByHref})
        <${escidocPropertiesNamespacePrefix}:name>#escT(${aggregationDefinitionName})</${escidocPropertiesNamespacePrefix}:name>
        #restLink(${structuralRelationsNamespacePrefix} "scope" ${aggregationDefinitionScopeTitle} ${aggregationDefinitionScopeHref})
    </${aggregationDefinitionNamespacePrefix}:properties>
#parse("aggregation-definition-new/rest/aggregation-table-list.vm")
#parse("aggregation-definition-new/rest/aggregation-statistic-data-selector.vm")
</${aggregationDefinitionNamespacePrefix}:aggregation-definition>
