#if ($reportDefinitionRoles)
    <${reportDefinitionNamespacePrefix}:allowed-roles>
#foreach( $reportDefinitionRole in $reportDefinitionRoles )
        #soapLink(${structuralRelationsNamespacePrefix} "role" ${reportDefinitionRole.id})
#end
    </${reportDefinitionNamespacePrefix}:allowed-roles>
#end