<${memberListNamespacePrefix}:member-list #virtualResourceRootForResourceList($memberListNamespacePrefix $memberListNamespace $memberListTitle)>
#foreach( $entry in $containerMembers )
${entry}
#end
</${memberListNamespacePrefix}:member-list>
